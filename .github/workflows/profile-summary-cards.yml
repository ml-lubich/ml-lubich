name: GitHub-Profile-Summary-Cards

# Disabled: Profile Summary cards were removed from the README in favor of the
# branded GitHub Stats / streak / trophy section (no Vercel rate-limit ERROR cards).
# Keep this workflow file as a no-op so old scheduled runs do not recreate junk SVGs.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Profile Summary cards retired — nothing to generate."
